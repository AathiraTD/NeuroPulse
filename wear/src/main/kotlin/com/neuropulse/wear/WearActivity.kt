package com.neuropulse.wear

import android.os.Bundle
import androidx.activity.ComponentActivity

/**
 * Placeholder Wear OS activity — replaced with full tile and notification setup in Phase 5.
 *
 * Phase 5: Add @AndroidEntryPoint, WearApplication (@HiltAndroidApp),
 * BreathingTile, TaskCompleteTile, and WearNotificationReceiver.
 */
class WearActivity : ComponentActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
